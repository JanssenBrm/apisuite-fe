import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
	palette: {
		background: {
			default: '#e5e5e5',
		},
		primary: {
			main: '#00915a',
		},
		secondary: {
			main: '#00e676',
		},
	},
})


export default theme
