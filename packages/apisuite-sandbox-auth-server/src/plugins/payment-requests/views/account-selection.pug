doctype html
html
  head
    title #{title}
    meta(charset='utf-8')
  body
    input#accountList(type='hidden', name='debtorName', value=accounts)
    input#paymentId(type='hidden', name='paymentId', value=id)
    input#clientId(type='hidden', name='clientId', value=clientId)
    input#brand(type='hidden', name='brand', value=brand)
    form(name='consent', action='/payment-requests/' + id + '/consent?client_id=' + clientId, method='post')
      div(class='form')
        input(type='hidden', name='next', value=next)
        #accounts
  script
    include ../../../../dist/js/accounts.js