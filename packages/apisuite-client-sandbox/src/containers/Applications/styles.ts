import { makeStyles } from '@material-ui/styles'

export default makeStyles(({
  root: {
    minHeight: '100%',
    backgroundColor: '#fff',
  },
  contentContainer: {
    maxWidth: 900,
    margin: '0 auto',
  },
}))
