import { Color } from '@material-ui/core'

export const colorPicker = (color: Partial<Color>,shade: number) => {
  console.log(Object.keys(color))
//   return color[closestShade]
}

// const findClosest = () = => {

// }
