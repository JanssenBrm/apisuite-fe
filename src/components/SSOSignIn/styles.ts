import { makeStyles } from '@apisuite/fe-base'

const useStyles = makeStyles({
  pleaseHoldMessage: {
    fontSize: '25px',
    fontWeight: 200,
    margin: '100px 0px',
    textAlign: 'center',
  },
})

export default useStyles
