import { makeStyles } from "@apisuite/fe-base";

export default makeStyles((theme) => ({
  contentContainer: {
    maxWidth: 915,
    width: "100%",
  },

  highlightIcon: {
    color: theme.palette.info.main,
    fontSize: 60,
  },
}));
