import { makeStyles } from "@apisuite/fe-base";

export default makeStyles({
  stack: {
    display: "flex",
    flexDirection: "column",
    position: "fixed",
    top: 42,
    width: "100%",
    zIndex: 1300,
  },
});
